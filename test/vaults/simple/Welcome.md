This is the vault used by the wdio-obsidian-service end-to-end tests (see `wdio.conf.mts`
and `test/specs/`). wdio-obsidian-service copies it into a temporary directory before each
test run, so nothing written here by the tests is ever persisted back to this file.
